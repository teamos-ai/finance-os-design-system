import { Section } from '@/showcase/Section'

export function NotionSection() {
  return (
    <Section id="notion" eyebrow="18 - Notion" title="Notion" lead="A Notion-style document template on-system - callouts, toggles, tables and code blocks.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}