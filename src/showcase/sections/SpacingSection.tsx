import { Section } from '@/showcase/Section'

export function SpacingSection() {
  return (
    <Section id="spacing" eyebrow="07 - Spacing & Layout" title="Spacing & Layout" lead="The 8px space scale, container widths, breakpoints and the grid primitives every surface inherits.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}