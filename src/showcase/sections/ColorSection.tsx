import { Section } from '@/showcase/Section'

export function ColorSection() {
  return (
    <Section id="color" eyebrow="05 - Color" title="Color" lead="Primitive ramps to semantic surfaces, per theme - each swatch labelled with its token and an AA badge.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}