import { Section } from '@/showcase/Section'

export function ComponentsSection() {
  return (
    <Section id="components" eyebrow="11 - Components" title="Components" lead="The interactive UI library - buttons, inputs, badges, controls - every variant, state and size.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}