import { Section } from '@/showcase/Section'

export function ElevationSection() {
  return (
    <Section id="elevation" eyebrow="08 - Radius & Elevation" title="Radius & Elevation" lead="8px-max squircles and the elevation language - soft shadows plus the signature coloured glow.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}