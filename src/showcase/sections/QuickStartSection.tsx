import { Section } from '@/showcase/Section'

export function QuickStartSection() {
  return (
    <Section id="quickstart" eyebrow="03 - Quick Start" title="Use it in five minutes" lead="Clone, import the tokens, wrap in the theme provider, drop in a starter component.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}