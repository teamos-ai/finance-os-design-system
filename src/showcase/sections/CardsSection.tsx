import { Section } from '@/showcase/Section'

export function CardsSection() {
  return (
    <Section id="cards" eyebrow="12 - Cards" title="Cards" lead="The card family - base, feature, tool, stat and dashboard-preview - padding, radius, glow and media.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}