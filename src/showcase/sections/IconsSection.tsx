import { Section } from '@/showcase/Section'

export function IconsSection() {
  return (
    <Section id="icons" eyebrow="10 - Icons & Logo" title="Icons & Logo" lead="The icon set, logo lockups and clear-space rules across every theme.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}