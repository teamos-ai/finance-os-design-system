import { Section } from '@/showcase/Section'

export function VideoSection() {
  return (
    <Section id="video" eyebrow="02 - Promo Video" title="The system in motion" lead="A tokenized video player with a squircle frame, custom controls and a reduced-motion fallback.">
      <p className="text-center font-mono text-caption text-fg-subtle">
        Reference in progress.
      </p>
    </Section>
  )
}